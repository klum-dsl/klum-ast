import com.blackbuild.klum.ast.gradle.convention.GroovyVersion

plugins {
    id 'com.blackbuild.convention.groovy'
    id 'groovy'
}

repositories {
    mavenLocal()
    mavenCentral()
}

groovyDependencies {
    groovyVersion = GroovyVersion.GROOVY_4
}

tasks.check {
    doLast {
        def result = configurations.groovy.incoming.resolutionResult
        def actual = result.allComponents
                .findAll { it.id instanceof ModuleComponentIdentifier }
                .collect { id -> "$id.id.group:$id.id.module:$id.id.version" as String }
                .toSet()

        assert actual == ["org.apache.groovy:groovy-bom:4.0.32", "org.apache.groovy:groovy:4.0.32"] as Set
    }
}