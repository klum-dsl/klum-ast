# Open issue index for the 4.0 release

Snapshot refreshed 2026-07-17: 109 open issues in `klum-dsl/klum-ast`, inspected from repository base `65f7a99b` and the live issue tracker. The original inventory was read-only; later confirmed normalization actions are recorded below.

All dispositions are provisional maintainer recommendations. “Relevant” means that the current source still lacks the requested capability or still contains the reported risk; it does not mean that the issue is accepted product policy. The historical 2.x/3.x sections of `docs/user/Roadmap.md` were not used as current policy.

## Evidence and compatibility keys

Each row cites one or more of these inspected evidence bundles. The issue body and comments are always part of the evidence (`GH`).

| Key | Inspected evidence |
|---|---|
| `BF` | [ADR 0003](../../adr/0003-builder-first-materialization.md), [Builder-first migration](../../user/Builder-First-Migration.md), `DSLASTTransformation`, `KlumBuilder`, `KlumModelProxy`, and `BuilderFirstSpec`. |
| `AB` | [ADR 0004](../../adr/0004-asbuilder-composition-protocol.md), its [implementation context](../adr-0004-asbuilder-composition.md), pending converter/factory tests, and PR #429 history. |
| `SCHEMA` | `DslAstHelper`, `FieldAstValidator`, `DSLASTTransformation`, `TransformSpec`, `InheritanceSpec`, and the annotation Javadocs. |
| `FACT` | `KlumFactory`, `FactoryHelper`, `AbstractFactoryBuilder`, `AlternativesClassBuilder`, `ConverterBuilder`, and factory/converter specs. |
| `TPL` | `TemplateManager`, `BoundTemplateHandler`, `TemplateMethods`, `TemplatesSpec`, `BoundTemplatesSpec`, and `docs/user/Templates.md`. |
| `COPY` | `OverwriteStrategy`, `Overwrite`, `CopyHandler`, `OverwriteStrategyTest`, and `docs/user/Copy-Strategies.md`. |
| `PHASE` | `PhaseDriver`, typed visiting actions, lifecycle phase implementations, `LifecycleSpec`, and `docs/user/Model-Phases.md`. |
| `PATH` | `BreadcrumbCollector`, `KlumBuilder`, `KlumModelProxy`, `DslHelper`, `ModelPathTest`, `BreadcrumbDSLTest`, and `docs/user/Exception-Handling.md`. |
| `L3` | `Cluster`, `Layer3`, `ClusterModel`, `StructureUtil`, their transformation/runtime tests, and `docs/user/Layer3.md`. |
| `REL` | ADRs 0003/0004/0005, `FieldType`, generated property/DSL accessors, `KlumBuilder` relationship normalization/materialization, Auto-Link phases, Layer 3 tests, and v3.0.1 API/docs fixtures. |
| `JACK` | `KlumAstModule`, `KlumAnnotationIntrospector`, `KlumDeserializer`, `JsonExportSpec`, and `docs/user/Jackson-Integration.md`. |
| `GRADLE` | Gradle plugin implementations, BOM, TestKit/scenario builds, and `docs/user/Gradle-Plugins.md`. |
| `IDE` | `PolymorphicMethods.gdsl`, its IntelliJ 142 dependencies, and the absence of an Eclipse DSLD implementation. |
| `DOC` | AnnoDoc generation/tests and the current user documentation/changelog. |
| `HIST` | Git history, notably PR #429, commit `fe9b3eff` (#335), `1a278c61` (#356), and `14fd628b` (partial #142 work). |

Compatibility abbreviations:

- `AST`: generated signatures/compiler behavior; verify Groovy 3, 4, and 5. Java 17 is the unchanged baseline.
- `RT`: runtime behavior; verify the affected module on all three Groovy lanes, with no expected Java-version delta.
- `J17/API`: changes Java-visible generated/public signatures and can be source/binary breaking.
- `Gradle`: needs plugin scenario coverage across supported Groovy versions and an explicit Gradle compatibility matrix.
- `IDE`: IDE-version-specific; Java/Groovy runtime lanes do not establish compatibility.
- `Docs`: no runtime compatibility impact.

The evidence key in each row supplies the relevant ADR/documentation/test set; directly related issue numbers are named in the row. A PR is named only where the issue discussion or local merge history establishes a direct relationship (principally PR #429). Where no PR is named, no direct PR was identified in the issue body/comments or local merge history; this is not a claim that no unlinked historical PR ever touched the same code.

## Inventory

| Issue | Problem / intended capability; module and public API | Kind | Current evidence, related work, and Builder-first relationship | Compatibility | Provisional disposition |
|---|---|---|---|---|---|
| #13 | Permit non-`String` `@Key` values; annotations, AST, `KlumFactory.Keyed`, generated key parameters, map semantics. | enhancement | **Relevant:** `SCHEMA` still emits a compile error unless the key field is `String`; glossary and public factories also assume `String`. This changes a foundational Builder/factory contract. | AST, J17/API | **5.0** |
| #14 | Add Eclipse DSLD support; IDE metadata/distribution. | enhancement | **Relevant:** `IDE` contains only an IntelliJ GDSL. No Eclipse DSLD source or test exists. Independent of Builder materialization, though generated Builder naming from #394 affects it. | IDE | **later 4.x** |
| #23 | Old IntelliJ GDSL crash calling `ClsClassImpl.getAnnotation`. | bug | **Likely obsolete:** stack is from the removed `ConfigDslIDESupportgdsl`; current `IDE` uses `PolymorphicMethods.gdsl` and different PSI calls. No current reproduction or affected code remains. Related #30. | IDE | **close as obsolete** |
| #30 | Nested GDSL elements stopped resolving in IntelliJ 2016.2. | bug | **Obsolete environment:** issue links JetBrains IDEA-162019 and names IntelliJ 2016.2; no current reproduction. Current GDSL is a different implementation (`IDE`). | IDE | **close as obsolete** |
| #43 | Infer a concrete subtype for `Create.From` from file naming/package conventions. | enhancement / design | **Still unimplemented:** `FACT` selects the declared/default type for delegating sources; regular scripts may return a subtype but File/URL names do not choose one. Builder-first requires any nested variant to use `AsBuilder` (#431). | AST, RT, J17/API | **later 4.x** |
| #63 | Validate `@Owner` target types during transformation. | enhancement | **Relevant but underspecified:** owner method arity and illegal key/owner combinations are checked, while compatible owner availability remains a runtime traversal concern (`SCHEMA`, `PHASE`). Needs a precise compile-time rule for polymorphic/transitive owners. | AST | **4.1** |
| #79 | Add URI-identified, resource-backed defaults for Simple Values; `@Default` public API and resolution extension seam. | enhancement / design | **Relevant and distinct:** current `@Default` supports field/delegate/code sources and already applies only when unset, but has no URI-backed source. The maintainer confirmed that the schema author supplies the URI and explicit model input wins. Raw resources and keyed properties are examples; YAML/TOML and URI details remain open. This supports incremental replacement of Helm and other `* as Code` inputs. Related, not duplicate: model-seeded external resolution (#314), resource-backed DSL Object recipes (#386), and whole-tree map input (#430). Evidence: issue #79 clarification, current `Default`/`DefaultPhase`, `docs/user/Default-Values.md`. | AST, RT, J17/API; all Groovy lanes | **later 4.x** |
| #83 | Let empty `apply` expose the RW object for post-construction mutation. | enhancement | **Contradicts accepted design:** completed DSL Objects expose no generated `apply`; configuration belongs to Builders (`BF`, `BuilderFirstSpec`). | J17/API | **close as obsolete** |
| #88 | Make read-only/completed models optionally writable. | design decision | **Contradicts ADR 0003:** structural immutability is the defining 4.0 boundary; only `TRANSIENT` state remains mutable (`BF`). | J17/API | **close as obsolete** |
| #91 | Generate model/RW interfaces plus implementations. | refactoring / design | **Superseded:** ADR 0003 chose a concrete generated Builder plus completed DSL Object; #394 owns the still-open generated-type layout question. | AST, J17/API | **close as obsolete** |
| #98 | Scope Templates to script execution instead of global state. | enhancement | **Implemented in substance:** `TPL` provides lexical `Template.With`/`WithAll`, thread-local restoration, script-created Templates, and tests for nested scope restoration. Builder-first rehydrates recipes per construction. | RT | **close as completed** |
| #115 | Split the central AST transform into independent metadata-coordinated transforms. | refactoring | **Relevant:** `DSLASTTransformation` still orchestrates helper builders from one transform (`SCHEMA`). No user-visible 4.0 requirement or ADR mandates the split. | AST | **later 4.x** |
| #117 | Prevent generated `toString()` from including `metaClass`. | bug | **Obsolete:** KlumAST stopped generating `toString()` in 2.2 (`CHANGES.md`); current canonical generation creates equality/hash behavior only. | AST | **close as obsolete** |
| #129 | Assert compilation error messages, not only exception types. | refactoring / tests | **Relevant:** many current specs still assert only `MultipleCompilationErrorsException`; newer Builder-first tests assert actionable messages. No public API impact. | AST test quality | **later 4.x** |
| #132 | Add a final configuration closure to `Create.From`. | enhancement | **Relevant:** `FACT` has no `From(..., Closure)` overload. Its PostApply timing question must be expressed as Builder configuration and must not create a second lifecycle (#431). | AST, RT, J17/API | **4.1** |
| #135 | Accept lists of Templates directly in collection-factory calls. | enhancement | **Relevant convenience gap:** collection factories accept one Template or an anonymous map; global `Template.WithAll(List, Closure)` exists (`TPL`). Builder semantics are already compatible. | AST, RT | **4.1** |
| #140 | Generate explicitly enabled alternative methods for a single DSL Object field. | enhancement | **Relevant:** named alternatives are generated for collection factories; single fields support type-selecting creation but not named alternatives (`FACT`, `docs/user/Alternatives-Syntax.md`). | AST | **4.1** |
| #142 | Inject arbitrary third-party annotations into generated members. | enhancement / design | **Uncertain:** commit `14fd628b` attempted Jackson-specific injection; the modern Jackson module uses an introspector instead (`JACK`, `HIST`). The general injection requirement and retention/target rules remain unspecified. | AST, J17/API | **needs maintainer decision** |
| #144 | Support maps whose values are lists/collections. | enhancement | **Relevant but outside current schema contract:** `BF`, `SCHEMA`, glossary, and copy docs explicitly say nested collections are unsupported. Supporting them changes generation, materialization, copy, and path semantics. | AST, RT, J17/API | **5.0** |
| #147 | Avoid silently replacing a prior map child with the same key. | bug / design | **Implemented by newer semantics:** issue #325 behavior merges repeated child configuration into the existing Builder; `TransformSpec` and `OverwriteStrategyTest` pin identity and merged fields. | RT | **close as completed** |
| #158 | Create an anonymous Template inline around a collection factory. | enhancement | **Implemented:** `AlternativesClassBuilder` generates the map-plus-closure overload and `docs/user/Templates.md` documents `servers(isCluster: true) { ... }` (`TPL`). | AST, RT | **close as completed** |
| #159 | Allow instance-style converter bodies and transform them into factories. | enhancement / design | **Relevant:** converters are still static/source factory methods (`FACT`). ADR 0004 adds Builder-producing variants but does not authorize rewriting instance methods. | AST, J17/API | **later 4.x** |
| #161 | Treat Groovy traits as DSL providers. | enhancement / design | **Relevant and high complexity:** issue comments record AST/trait limitations; no trait Builder generation exists (`SCHEMA`). This affects inheritance and generated public types. | AST, J17/API | **5.0** |
| #165 | Avoid retaining an RW object after construction. | refactoring | **Implemented more strongly:** completed models retain only `KlumModelProxy`; Builders are construction-session objects and are not retained (`BF`, serialization tests). | RT | **close as completed** |
| #166 | Move synthetic Template/proxy state off the model into dedicated construction/support objects. | refactoring | **Implemented by #431:** Builder state is on `KlumBuilder`, completed state on `KlumModelProxy`, and Template-only deferred state on `KlumTemplateProxy`/immutable recipe state. | RT, J17/API | **duplicate** of #431 |
| #168 | Permit maps of unkeyed DSL Objects. | enhancement | **Implemented with explicit key mapping:** `@Field.keyMapping` supports unkeyed values, with transform/runtime tests and user-documentation examples (`SCHEMA`). | AST, RT | **close as completed** |
| #174 | Verify copied parameter annotations. | refactoring / tests | **Implemented:** `ParameterAnnotation.ClosureHint` has reflection assertions for `@DelegatesTo` and `@ClosureParams` in `TransformSpec`; docs describe the contract. | AST | **close as completed** |
| #179 | Include fields of a non-DSL superclass in a DSL schema. | enhancement / design | **Relevant:** Builder inheritance mirrors DSL Object inheritance only; non-DSL state has no accepted materialization contract (`BF`, `SCHEMA`). | AST, J17/API | **5.0** |
| #180 | Preserve DSL class type parameters in generated Builders/factories. | enhancement / bug | **Relevant:** generated Builder classes are not declared with the model's generic parameters and there is no generic-model coverage (`SCHEMA`). Public signatures and inheritance would change. | AST, J17/API | **5.0** |
| #182 | Treat custom concrete collection/map subclasses as schema collections. | enhancement | **Conflicts with accepted 4.0 contract:** ADR 0003 deliberately rejects concrete/custom collection declarations and requires supported interfaces (`BF`, `BuilderFirstSpec`). | AST, J17/API | **close as obsolete** |
| #183 | Support generic converter methods. | enhancement | **Relevant:** `ConverterBuilder` warns for unmapped placeholders but has no complete generic-converter contract. Closely related to #431's source-visible Builder twins, but not included in ADR 0004 acceptance. | AST, J17/API | **4.1** |
| #184 | Generate Template-like methods/fields from a declarative mapping macro. | enhancement / design | **Relevant but speculative:** no such annotation exists; current Templates are object recipes, not source-code macros (`TPL`). Overlaps #375/#386. | AST, J17/API | **later 4.x** |
| #185 | Configure owner assignment as set-once/reset/fail and allow collection owners. | enhancement / design | **Relevant:** owner phase currently applies its existing matching/assignment rules and has no `SetType` API (`PHASE`, owner tests). Builder-first makes this a pre-materialization policy. | AST, RT | **later 4.x** |
| #190 | Allow protected creation so a type is constructible only through custom factories/converters. | enhancement / design | **Relevant:** generated root factory entry points remain public; field protection controls relationship methods, not root construction (`FACT`). Needs Java/Groovy visibility and inheritance semantics. | AST, J17/API | **later 4.x** |
| #193 | Return construction counterparts from RW getters, including relationship collections. | enhancement | **Implemented by Builder-first:** Builder relationship storage and child methods use Builders, and completed models expose materialized DSL Objects (`BF`, `PropertyAccessors`, `BuilderFirstSpec`). | AST, RT | **close as completed** |
| #201 | Use target key-field names consistently in generated parameters and avoid local-name collisions. | enhancement / refactoring | **Partly implemented:** relationship methods use the actual key field name; reflected factory methods preserve source names. There is no focused acceptance test covering all generated factories/collisions (`SCHEMA`, `AstReflectionBridgeTest`). | AST, J17/API | **4.0 nice-to-have** |
| #202 | Generate convenience factories for DSL interfaces. | enhancement | **Relevant:** abstract/default implementations and artificial Template implementations exist, but no demonstrated interface factory contract (`FACT`, `BF`). #431 must not accidentally imply root-instantiability. | AST, J17/API | **4.1** |
| #204 | Make model constants/static members directly visible in Builder method bodies. | enhancement | **Relevant:** no import/delegation step copies static members to Builders (`SCHEMA`). Fully qualified access remains available. | AST | **later 4.x** |
| #205 | Improve dynamic missing-method diagnostics to name the Builder/DSL operation and useful candidates. | bug | **Provisionally relevant:** Builder-first adds targeted lifecycle/adoption errors, but no current focused typo test replaces this report (`FACT`, `BF`). A current 4.0 reproduction is needed. | RT, Groovy dynamic dispatch | **4.0 nice-to-have** |
| #207 | Aggregate multiple classpath model descriptors/components. | enhancement / design | **Relevant:** `Create.FromClasspath` reads one descriptor and returns one model (`FACT`, Gradle descriptor generation). Related #338. | RT, J17/API | **later 4.x** |
| #208 | Stop delegating ordinary model methods onto the generated construction type. | breaking refactoring | **Still relevant but contrary to current compatibility behavior:** `DelegateFromRwToModel` deliberately projects non-private model methods onto Builders; mutators are moved separately. Changing this is a broad generated-API break. | AST, J17/API | **5.0** |
| #209 | Return every child created inside a collection-factory block. | enhancement | **Relevant:** individual child methods return Builders, but the outer collection closure returns its closure result rather than a defined list (`FACT`, `TransformSpec`). | AST, RT | **later 4.x** |
| #218 | Make RW only an interface and use a dynamic proxy as the mutable implementation. | refactoring / design | **Superseded:** ADR 0003 requires real generated Builders holding construction state; #394 owns their final layout (`BF`). | AST, J17/API | **close as obsolete** |
| #226 | Supply Gradle/Maven schema/model tooling, descriptors, dependency alignment, and tests. | enhancement | **Implemented:** schema/model/convention plugins, BOM constraints, descriptors, dependency inheritance, sources/Javadocs, and scenario tests exist (`GRADLE`). Remaining “included models” work is separately #337. | Gradle | **close as completed** |
| #240 | Add Klum exclusions to a user-supplied `@EqualsAndHashCode`. | bug / enhancement | **Provisionally relevant:** when the annotation exists, Klum skips its own equality generation and does not amend exclusions; generated companion/owner/transient fields must not affect equality (`SCHEMA`). Current Groovy may already ignore synthetic fields, so reproduction is required. | AST, all Groovy lanes | **4.0 nice-to-have** |
| #261 | Sandbox model scripts like Jenkins' Groovy sandbox. | enhancement / security design | **Relevant but undefined:** factory scripts execute as Groovy code; no sandbox module or policy exists (`FACT`). The issue itself suggests an optional facility and provides no threat model. | RT, Groovy compiler/runtime, J17 security APIs | **later 4.x** |
| #265 | Add schema-level feature toggles to omit generated capabilities. | enhancement / design | **Relevant and broad:** `@DSL` has no general feature set; Builder-first intentionally establishes a stable generated API (`BF`). Per-class/package/compilation semantics remain undecided. | AST, J17/API | **5.0** |
| #269 | Encode the target model/Builder type in delegating scripts for IDE/static typing. | enhancement / design | **Relevant:** sources are compiled with `DelegatingScript`, while the model-specific type is supplied at runtime; GDSL support is limited (`FACT`, `IDE`). ADR 0004 will make the Builder delegate more important. | AST, IDE, all Groovy lanes | **4.1** |
| #281 | Guard creation/application against illegal lifecycle phases. | enhancement / breaking behavior | **Partly implemented:** typed phase actions enforce the Builder/Model side of `INSTANTIATE`, and nested root factories are rejected; general create/apply phase guards remain explicitly deferred by ADR 0003/0004 (`PHASE`, `AB`). | RT | **4.1** |
| #282 | Resolve conflicts between a real field and an `@Field` virtual setter method. | bug / design | **Relevant:** collection member-name collisions are checked, but no general real-field/virtual-field precedence rule exists (`SCHEMA`). This can change generated method availability. | AST, J17/API | **4.0 nice-to-have** |
| #287 | Experiment with AsciiDoc conversion for Javadoc tests. | refactoring / experiment | **Obsolete:** current AnnoDocimal extraction and generated `@AnnoDoc` tests are established; the issue records an abandoned doclet workaround (`DOC`). | Docs/test only | **close as obsolete** |
| #304 | Coordinate ordered source-neutral configuration layers. | enhancement | **Confirmed canonical coordinator:** generated `Create.Compose(KlumConfigurationLayer<T>...)` owns one root Builder lifecycle, ordered heterogeneous layers, transient per-layer diagnostics, and atomic publication. `KlumLayers` hands off copy/Map/DelegatingScript/text/file/URL sources; Jackson contributes exactly one input. Schema policy remains default; #310 owns layer-local runtime-policy semantics. #132/#342 delegate convenience syntax, #432/#402 own durable provenance. | AST, RT, JACK, J17/API; Groovy 3/4/5 | **4.1, ready-for-human API review** |
| #305 | Add deterministic declarative phase registration; defer schema annotation syntax. | enhancement / design | **Accepted in ADR 0008:** state-typed registration providers have stable IDs, fresh actions, numeric phase authority, equal-phase dependencies, pre-construction validation, and a legacy adapter. A schema `@CustomPhase` annotation remains deferred. | RT, J17/API | **later 4.x, planned** |
| #310 | Override copy strategy for one Template/copy run. | enhancement | **Relevant:** strategy lookup is annotation/class/package based; no per-run strategy input exists (`COPY`). #350 asks for the same runtime/scoped capability. | RT, J17/API | **4.1** |
| #314 | Define an external Value Provider seam that resolves a target from a separate model-supplied seed; provider API, generated access/storage, and lifecycle integration. | enhancement / design | **Relevant and distinct:** no provider API/module exists. The maintainer confirmed separate seed/target properties, normal model read access, non-serialization by default, no action without a seed, and construction failure with provider/target context when a supplied seed cannot resolve. Provider implementations belong outside core. Creation-time versus phase-based resolution, syntax, and transient-versus-companion storage remain open. If phase-based, it follows `AUTO_CREATE`/`AUTO_LINK`; after `POST_TREE` and before `INSTANTIATE` is the recorded recommendation, with the `POST_TREE` visibility trade-off. Related to #79 but not a default mechanism. Evidence: issue #314 clarification and lifecycle phase order (`PHASE`). | AST, RT, J17/API; all Groovy lanes | **later 4.x** |
| #331 | Remove all generated methods/fields from DSL Object types, including `Create`, in favor of a global facade. | breaking design | **Superseded/contradicted:** ADR 0003 removes completed-model mutation but explicitly retains generated `Create`, `Template`, and relationship syntax as the stable client interface (`BF`). | J17/API | **close as obsolete** |
| #335 | Eliminate flaky AnnoDoc test for auto-overridden factory methods. | bug / tests | **Implemented:** commit `fe9b3eff` removed `@Retry` and reads generated `@AnnoDoc`; current test retains that approach (`HIST`, `DOC`). | AST tests, Groovy 3/4 | **close as completed** |
| #337 | Build a combined model by including sources from another model project. | enhancement | **Relevant:** Gradle plugins support schema dependencies and one model's descriptors, not source inclusion/combined Javadoc or subclass discovery (`GRADLE`). | Gradle, AST | **later 4.x** |
| #338 | Make multi-classpath creation return a list. | enhancement | **Same unresolved capability as #207:** no `CreateMultipleFromClasspath` exists; this issue only proposes the result shape. | RT, J17/API | **duplicate** of #207 |
| #342 | Combine Template/copy input with child/root configuration syntax. | enhancement / design | **Relevant:** Template scope and `copyFrom` work separately; no `Create.With(Template,...)`/`child(template){}` API exists (`TPL`, `COPY`). Must rehydrate, never adopt, under Builder-first. | AST, RT, J17/API | **4.1** |
| #343 | Explicitly mark Template objects and reject direct relationship assignment. | enhancement / bug prevention | **Implemented by #431:** ADR 0004 folds graph-aware Template identity and relationship rejection into #431's accepted behavior. | RT, J17/API | **duplicate** of #431 |
| #344 | Add typed/filtered `StructureUtil.visit`. | enhancement | **Relevant:** `deepFind` filters by type, while `visit` invokes a general `ModelVisitor`; no typed visit overload exists (`L3`). Must preserve cycle and composition-only traversal. | RT, J17/API | **later 4.x** |
| #349 | Allow user-defined copy strategy implementation classes. | enhancement / design | **Relevant:** copy policy is closed enums interpreted by `CopyHandler`; no extension SPI exists (`COPY`). Needs Builder-safe semantics and serialization rules. | RT, J17/API | **later 4.x** |
| #350 | Add per-run or scoped runtime copy strategies. | enhancement | **Duplicate intent:** this is the same missing runtime override described by #310 and has no additional body/criteria (`COPY`). | RT, J17/API | **duplicate** of #310 |
| #352 | Add “fail if target not empty” strategies for maps/single values. | enhancement | **Relevant:** current enums offer replace/merge/set-if-empty but no fail-on-existing choice (`COPY`). | RT, J17/API | **later 4.x** |
| #356 | Infer fixed Layer 3 keys from field names. | enhancement / design | **Not on the main branch:** commit `1a278c61` exists only on `feature/356-fixed-keys`; current master has no `@Layer3` annotation and the issue remains open. #454 now owns sharpening the Layer 3 concept and documenting this status, but does not decide the fixed-key behavior. | AST, RT, J17/API | **needs maintainer decision** |
| #357 | Flatten list/map members into Cluster paths. | enhancement | **Relevant:** `ClusterModel.getCollectionsOfType` can collect collection-valued members, but does not publish flattened `list.index`/`map.key` entries (`L3`). | AST, RT | **4.1** |
| #358 | Rework Jackson deserialization through copy/Builder semantics and choose phases. | enhancement / design | **Implementation completed by PR #429:** `KlumDeserializer` restores maps through `FactoryHelper.createFromSerializedState` and the normal Builder lifecycle (`JACK`, `BF`). The remaining policy decision is explicitly #428; renamed property mapping remains #251. | JACK, RT | **close as completed** |
| #371 | Reject or define shadowed fields in DSL inheritance, especially owners/defaults. | bug / schema validation | **Provisionally relevant:** no general duplicate-property-in-hierarchy check exists; Builder fields/materialization iterate hierarchy-specific state (`SCHEMA`, `BF`). Body lacks a reproducible example. | AST, RT, all Groovy lanes | **4.0 nice-to-have** |
| #372 | Handle an explicit Groovy dependency when Spock is not explicitly configured. | bug | **Implemented:** the convention plugin derives Spock from Groovy and supports `skipSpock`; explicit/no-Spock scenarios exist, including Groovy 5 (`GRADLE`). | Gradle | **close as completed** |
| #375 | Add an Ensemble annotation that copies existing fields/classes/methods. | enhancement / design | **Overlaps #184:** both describe declarative generation/copying of fields and methods; neither defines a stable semantic distinction. Templates themselves copy values, not schema members (`TPL`). | AST, J17/API | **duplicate** of #184 (provisional) |
| #379 | Replace repeated proxy-method generation code with an internal annotation. | refactoring | **Relevant internal cleanup:** generation still calls `ProxyMethodBuilder` explicitly throughout the AST transform. No public behavior or release dependency is stated. | AST | **later 4.x** |
| #382 | Decide logging versus a framework-independent trace mechanism. | design decision | **Superseded by broader #402 framing:** its only comment prefers explicit traces; #402 covers lifecycle events, validation, breadcrumbs, and storage (`PATH`). | RT, J17/API | **duplicate** of #402 |
| #386 | Declare field/class-scoped Templates through repeatable annotations. | enhancement / design | **Relevant:** no such annotation exists. Current `Template.With` is lexical runtime scope, not field metadata (`TPL`). Overlaps the macro direction of #184 but has distinct value-recipe semantics. Related #79 could supply an analogous URI/resource resolution seam for DSL Object recipes, while #79 itself remains limited to Simple Values. | AST, RT | **later 4.x** |
| #389 | State which Gradle versions the plugins support. | documentation / compatibility | **Relevant:** the user documentation describes plugin features but no supported Gradle range; scenarios run only the build's current wrapper (`GRADLE`). | Gradle, Docs | **4.0 nice-to-have** |
| #390 | Define a stable completed-Object support facade and internalize companion state. | design decision / documentation | **ADR 0006 OS-1/OS-2/OS-3 implemented:** `KlumObjectSupport.of` serves any root/subtree with construction and structural model paths plus grouped structure and stored-validation helpers. `getConstructionPath()` is its sole public construction-string getter; `BreadcrumbCollector` remains implementation vocabulary. `KlumModelProxy` and raw metadata are internal; provenance/lineage and applied-Template history are deferred. | J17/API, RT | **4.0 API-freeze evidence complete** |
| #391 | Define stable Java modules and finalize the handwritten public package surface. | breaking refactoring / design | **Confirmed 4.0 contract:** remove `com.blackbuild.groovy.configdsl` without aliases, eliminate split packages, consolidate schema vocabulary under `com.blackbuild.klum.ast`, expose a positive runtime allowlist, retain existing artifacts/GAVs and classpath support, and assign stable module names. A Groovy 3/4/5 JPMS prototype gates the ADR and tracer-bullet implementation plan; #450 coordinates upstream dependencies. Layer 3 remains a modeling pattern, not a package boundary. | J17/API, AST, RT, JACK, Gradle; all Groovy lanes | **4.0 must, design confirmed** |
| #394 | Generate the top-level `Foo_DSL` public interface namespace and remove RW vocabulary. | enhancement / design decision | **Accepted in ADR 0005, partially implemented:** `Factory`, `Builder`, Collection factories, and Cluster factories are nested public interfaces with AnnoDocimal IDE-only source mirrors. `KlumBuilder` narrows; `KlumRwObject`/`$_RW` go; deprecated `@DelegatesToRW` normalizes to `@DelegatesToBuilder`. DSL-G provides an explicit compile-then-refresh task and IDEA-only generated root while excluding mirrors from compilation, packaging, publication, classpaths, and downstream builds. | AST, Gradle, J17/API, IDE; all Groovy lanes | **4.0 must, planned** |
| #399 | Fix strange breadcrumbs for `PostCreate`. | bug | **Insufficient current evidence:** issue has no reproduction/expected path. `PATH` has extensive current PostCreate/Breadcrumb coverage, and Builder-first changed creation ownership. Needs a focused current case before disposition. | RT | **needs maintainer decision** |
| #402 | Unify logs/traces/validation as stored lifecycle events and clarify object versus breadcrumb paths. | enhancement / breaking design | **Relevant but broad:** current companion stores construction/structural paths and validation metadata, not a general event log (`PATH`). It would alter serialization, memory, and public diagnostics. #390 resolves current path terminology but deliberately defers provenance/lineage and applied-Template history here. | RT, J17/API | **5.0** |
| #406 | Compile-time reject shortcut `Validator.addIssue/addError` outside validation methods. | bug prevention / enhancement | **Relevant:** runtime validator context enforces the rule; transformation/type-checking extensions do not recognize these calls (`SCHEMA`, validation source/tests). | AST, all Groovy lanes | **4.1** |
| #414 | Add `@Mixin` to copy missing nested values from an owner/reference. | enhancement / design | **Relevant:** `COPY` can implement the behavior procedurally with `SET_IF_NULL`/`MERGE`, but no owner-aware annotation or lifecycle integration exists. Must copy into fresh child Builders. | AST, RT | **later 4.x** |
| #417 | Support Java records as DSL Objects with external transient/owner state. | enhancement / design | **Relevant:** DSL transformation assumes generated subclass/state/accessor patterns incompatible with record finality; Builder-first helps construction but not record inheritance/component rules (`BF`, `SCHEMA`). Java 17 makes records available. | J17/API, AST; all Groovy lanes | **5.0** |
| #420 | Group lifecycle methods into dedicated lifecycle classes. | enhancement | **Now technically enabled but unimplemented:** issue waited for #416; lifecycle callbacks through POST_TREE now execute on Builders (`BF`, `PHASE`). Annotation/discovery/inheritance API remains undefined. | AST, RT | **4.1** |
| #427 | Warn about mutable Simple Values that weaken deep immutability. | enhancement / diagnostics | **Relevant but explicitly non-blocking:** ADR 0003 guarantees structural, not deep, immutability; no compiler diagnostics exist (`BF`). | AST, all Groovy lanes | **4.1** |
| #428 | Deliver Jackson interoperability for immutable DSL Objects. | design decision / compatibility | **Accepted in ADR 0009, partially implemented:** #439/#440 provide resolved-property Builder binding, same-session identity, and customization groundwork. #463 must add explicit importer modes/breadcrumbs; #464 must prove asymmetric YAML import/export and close compatibility/docs. No Klum wire metadata or round trip exists. | JACK, RT, J17/API | **4.0 must, #463/#464 planned** |
| #431 | Implement `AsBuilder`, projections/twins, Template companion/copy sources, and the materialization scheduling boundary. | enhancement / compatibility fix | **Implemented:** #436 provides active sessions, #437 generated projections/twins and restored adaptable composition, and #438 Template companions, copy-source modes, and the strict scheduling boundary. AB-4 reconciles the public migration/release evidence with Java, static-Groovy, and Groovy 3/4/5 coverage; regular materializing Scripts remain intentionally top-level-only. | AST, RT, J17/API; Groovy 3/4/5 | **4.0 implementation evidence complete** |
| #432 | Expose applied-Template provenance through `KlumObjectSupport`. | enhancement / diagnostics design | **Deliberately deferred:** `getAppliedTemplates()` would expose richer lineage, not the single construction-path String. #390 does not absorb it; #402 owns the future applied-recipe/event-history placement. | RT, J17/API, PATH | **needs maintainer decision** |
| #435 | OS-1: expose completed-object construction-path and structure support. | enhancement / breaking implementation | **Implemented:** Java-first root/subtree facade, paths, owners, relative paths, typed traversal, and cycle-safe composition semantics without exposing the companion. The final public construction-path spelling is completed by #390 OS-3. | RT, J17/API | **4.0 must, implementation complete** |
| #441 | PH-1: add deterministic state-typed phase registration. | enhancement / plugin SPI implementation | **Ready later-4.x tracer bullet for #305/ADR 0008:** covers typed registrations, fresh actions, legacy adapter, deterministic dependency ordering, and pre-construction validation. | RT, J17/API | **later 4.x, ready-for-agent** |
| #450 | Audit KlumCast and AnnoDocimal integration opportunities before 4.0. | investigation / dependency design | **Synthesis prepared:** the [consumer inventory](issue-450-consumer-and-governance-inventory.md) and [final integration audit](issue-450-integration-audit.md) reconcile both merged upstream baselines. #459 and #461 are 4.0 blockers; #460 is desirable before 4.0; explicit no-change findings remain local/post-4.0. The issue is open until the audit PR is accepted and every relationship is discoverable. | AST, Gradle, IDE, JPMS; all Groovy lanes | **4.0 prerequisite, in review** |
| #453 | Investigate and define supported KlumAST extension mechanisms. | investigation / design | **Intentionally open-ended:** inventory public-looking hooks and real use cases, then support, redesign, defer, internalize, or reject each seam. No third-party extensions are currently known; #391 promises no general SPI by default. Related #305/#314/#394. | J17/API, AST, RT, Gradle; all Groovy lanes | **unmilestoned, needs triage** |
| #454 | Sharpen the Layer 3 concept and documentation. | documentation / domain model | **Baseline confirmed, grilling still required:** Layer 3 is the API–Schema–Model pattern, not a package/module boundary. Domain API Developer, Schema Developer, Client Developer, and Model Writer are distinct project roles; a later session must sharpen variants, dependency direction, examples, and specialized support before broad documentation work. | Docs | **unmilestoned, needs triage** |
| #455 | Revisit and align the multi-Groovy build approach across Klum libraries. | investigation / build design | **Separate from #450:** compare the three repositories' build/test/release evidence and decide shared versus aligned-local conventions. The integration audit supplies facts but does not choose the design or drop a Groovy generation. | Gradle, CI; all Groovy lanes | **unmilestoned, needs triage** |
| #456 | Introduce versioned user documentation and Javadocs. | documentation / release infrastructure | **Decision recorded in ADR 0013:** current 4.x authoring moves to docs/user/; tagged wiki/ remains historical input and the mutable wiki only supplies migration stubs. Protected GitHub Pages renders immutable exact documentation and isolated module APIs; every page has explicit archived/RC status chrome, aliases are proof-gated, and no public moving development tree exists. A versioned Season/logo manifest is captured in each exact tree and requires branding-owner approval before final rendering; later logo replacement is an explicit correction, not a snapshot rewrite. A post-proof status sidecar may point a public RC at its final without rewriting the immutable snapshot. The credential-free tracer precedes an independently validated pending Pages stage before #488 artifact publication. The confirmed VD-1 seam is a repository-local `buildSrc` renderer with root tasks, commit-object input, canonical manifest, hermetic fixtures, and a Pages-to-#488 evidence-only handoff; its portable contract does not extract shared code before a later explicit decision. AnnoDocimal #71 and KlumCast #47 are linked local counterparts, not shared implementation owners. | Docs, release | **4.0 must, cross-cutting implementation candidate** |
| #459 | Adopt the KlumCast 0.4 module-compatible artifact set. | dependency / JPMS integration | **Confirmed downstream gate from #450:** adopt the first 0.4.x satisfying `klum-cast#12/#24`, including the three-artifact graph, exclusive package ownership, stable automatic module names, service loading, and classpath behavior required by #391. | AST, J17/API, JPMS; all Groovy lanes | **4.0 blocker, ready-for-agent** |
| #460 | Migrate KlumAST custom checks to the KlumCast 0.4 SPI. | compiler integration / migration | **Confirmed desirable follow-up from #450:** move all eight checks from the deprecated adapter to the durable immutable-context/structured-diagnostic SPI while retaining supported name bindings and KlumAST ownership of its rules. | AST; all Groovy lanes | **pre-4.0 desirable, ready-for-agent** |
| #461 | Migrate KlumAST to AnnoDocimal 1.0 supported APIs and projection task. | dependency / Gradle / documentation integration | **Confirmed downstream gate from #450:** final AnnoDocimal 1.0 precedes KlumAST 4.0. Migrate provisional helpers, reuse the deterministic/configuration-cache-safe task, consume stable module names, and preserve KlumAST-owned IDEA/mirror policy. | AST, Gradle, IDE, JPMS; all Groovy lanes | **4.0 blocker, ready-for-agent** |
| #463 | Add explicit Jackson importer modes and construction paths. | enhancement / breaking API | **Confirmed JSON-3 tracer:** finalize the public importer signatures, then implement explicit root, Template, active-session Builder, and apply-to-Builder modes with caller-owned Jackson configuration and `KlumModelException` construction-path/cause preservation. Managed import contributes an import source; it does not promise provenance or lineage. Every operation binds exactly one `KlumJacksonInput`; #304 owns future ordered heterogeneous composition. | JACK, RT, J17/API; Groovy 3/4/5 | **4.0 blocker, ready-for-human API review** |
| #464 | Prove asymmetric Jackson/YAML interoperability. | enhancement / compatibility closure | **Confirmed JSON-4 tracer:** add separate foreign-import and external-export fixtures plus a documentary single-input YAML read/enrich/write workflow; synchronize role-oriented user documentation, migration, CHANGES, and compatibility lanes without a round-trip contract. Ordered multi-source composition remains #304 work. | JACK, Docs; Groovy 3/4/5 | **4.0 blocker, ready-for-agent; blocked by #463** |
| #467 | Define framework-owned public-interface naming and entrypoint conventions. | design / API convention | **Decision recorded in ADR 0010:** framework constructors, `of`, `using`, root-versus-Builder results, extension naming, intentional predicates, and visibility boundaries are fixed without reshaping generated DSL methods. `KlumObjectSupport.of(...).getConstructionPath()` is the sole future public construction-string getter; `BreadcrumbCollector` is implementation vocabulary. #474 supplies the narrow dynamic Builder `link(fieldName, target)` capability, while #474 retains its generated method shapes. | J17/API, AST; Groovy 3/4/5 | **4.0 decision complete; #390/#394/#431/#463/#474 deliver their owned APIs; #468 inventories them** |
| #468 | Finalize the 4.0 JVM-public type/member and generated-surface inventory. | compatibility / release gate | **Relevant late gate:** every public delta since v3.0.1 needs a classified owner and export treatment. It must freeze `KlumObjectSupport.getConstructionPath(): String` as a client entrypoint and classify `BreadcrumbCollector` as generated/runtime implementation linkage. #474 adds relationship-mode accessors and completed-model link batches that #468 must inventory; #468 does not choose their behavior. | J17/API, AST; Groovy 3/4/5 | **4.0 must, late API-freeze gate** |
| #469 | Provide task-oriented KlumAST 4.x adopter onboarding and portable Agent Skills. | documentation / onboarding design | **Confirmed 4.0 documentation scope:** versioned documentation is authoritative and portable, task-oriented agent skills enact it for Gradle adopters. The parent owns the two-axis entry decision (domain-first/target-contract and Layer 3/direct schema), a separate `agent-skills/` distribution, and field-test bounded preview. #470–#472 are its independently verifiable tracer bullets. Related but not a substitute for #456 versioned documentation or #454 Layer 3 vocabulary. | Docs, Gradle, L3; Groovy 3/4/5 | **4.0 must, parent scope confirmed** |
| #470 | Establish the Gradle onboarding baseline and portable adopter-skill distribution. | documentation / executable onboarding | **Ready tracer for #469:** deliver an entry guide, `start-klum-project` and `author-klum-model` skills, a minimal executable Gradle fixture, supported-Groovy/IntelliJ guidance, and a portable open-skills distribution. It must use versioned documentation as the authority. | Docs, Gradle, IDE; Groovy 3/4/5 | **4.0 must, ready-for-agent** |
| #471 | Teach domain-first modeling through an executable smart-home journey. | documentation / Layer 3 acceptance fixture | **Ready child of #469, blocked by #470:** prove a domain-first Layer 3 schema, configured model, and thin downstream adapter through shared executable documentation/skill evidence. It must use #454’s vocabulary without treating an example as unreviewed Layer 3 policy. | Docs, L3, Gradle; Groovy 3/4/5 | **4.0 must, ready-for-agent; blocked by #470** |
| #472 | Teach target-contract modeling through an executable Helm journey. | documentation / target-contract acceptance fixture | **Ready child of #469, blocked by #470:** prove direct-schema target-contract modeling with representative Helm values, meaningful defaults/validation/convenience, and golden-contract checks. It must not imply a universal import/export round trip or absorb #79/#304. | Docs, Gradle; Groovy 3/4/5 | **4.0 must, ready-for-agent; blocked by #470** |
| #474 | Preserve Layer 3 composition-or-aggregation relationships. | enhancement / breaking compatibility | **Implemented by PR #506:** `OPTIONAL_LINK` lets a relationship entry be composition or aggregation; `@LinkTo` selects it, while explicit `LINK` remains aggregation-only. Ownership is determined by same-session Builder composition claim versus an existing completed/claimed target. The delivered behavior includes generated Java/Groovy relationship method shapes, non-destructive dynamic Auto-Link fallback, collection/map semantics, migration, and ADR 0003/0004 clarification. It remains distinct from #431’s Builder-producing composition, #467’s framework naming convention, and #468’s final inventory. | AST, RT, L3, REL, J17/API; Groovy 3/4/5 | **4.0 implementation evidence complete** |
| #483 | Define the Season 4 KlumAST/KlumCast visual family. | product branding / release finalization | **Confirmed visual-only direction:** `Season 4: The Makeover` pairs with semantic `4.0`; KlumAST and KlumCast share a visual system but retain distinct accessible marks and technical identities. Stephan owns final manifest/correction approval. ADR 0013/#456 validate and capture the local asset, digest, alternative text, and approval; #488 retains publication authority. AnnoDocimal remains a policy-aligned independent peer. | Docs, release | **4.0 visual finalization, ready-for-human; not an RC-train blocker** |

## Inventory checks

- #483 was added during the 2026-07-21 branding normalization. Refresh the open-issue count before a later full inventory
  sweep. Completed #251/#383/#411/#430/#447 remain absent; previously omitted #467–#472 were restored to the inventory and
  #474 was added.
- No external PR was treated as a request surface, per `docs/agents/issue-tracker.md`.
- Remaining “close” recommendations are evidence-backed proposals only. The confirmed 2026-07-14 architecture normalization
  created milestone 4.0, updated #251/#305/#390/#394/#428/#431, and created #433–#441. The 2026-07-15 pass refined
  #391, created #453/#454, and linked `blackbuild/anno-docimal#36` through #450. The first 2026-07-16 pass reopened #450
  after PR #457's accidental mechanical state change and created native sub-issues #459–#461. The Jackson pass then
  normalized #428/#454, completed #251/#430/#447, and created #463/#464 with a native dependency. The 2026-07-17
  relationship pass created #474 and linked its ownership to #431/#467/#468; the inventory refresh also restored the
  already-live #469–#472 onboarding cluster. The construction-path pass updated #390/#463/#467/#468, retained #432 for
  future lineage placement, and completed #411. Implemented/completed issues are absent from this open-only table.
