# Issue 491 — Documentary-test audit inventory

Tracking issue: [#491 — Audit DSL documentation for documentary test coverage and traceability](https://github.com/klum-dsl/klum-ast/issues/491)

This is the repository-side inventory for the deliberate documentary-test audit. It records the evidence available on
`master` and avoids reclassifying legacy regression tests as user documentation merely because they exercise the same
implementation. The current 4.0 documentation root is `docs/user/`; #456 owns its eventual versioned URL and hosting
policy. Each record maps a user-visible documentation element or feature to readable executable evidence, or records a
concrete exception or follow-up. It does not require a separate documentary test for every individual code fence.

## Existing documentary evidence

The following aligned examples are already present in the executable suite:

| Documentation element | Responsible issue | Executable evidence | Audit result |
| --- | --- | --- | --- |
| [`Layer3.md` — Automatic creation and linking](../user/Layer3.md#automatic-creation-and-linking) | #474 | `OptionalLinkRelationshipTest.optional relationships retain local composition and aggregation identity for single List and Map entries` | Already aligned with `@Issue`, `@Tag("documentary")`, and `@See`. #454 owns the wider Layer 3 terminology rewrite. |
| [`Basics.md` — Factory construction](../user/Basics.md#factory-construction) | #76 (closed: move creator methods into a creator class) | `FactoryTest` exercised the generated factory but did not provide a readable linked documentary example. | Aligned by this slice with `FactoryConstructionTest.builds a completed deployment configuration with Create.With`. |
| [`Default-Values.md` — Default Values overview](../user/Default-Values.md#default-values) | #318, #361, #370 | `DefaultValuesDocumentaryTest` | Overview exception: this orienting heading has no independent API path; its immediately linked cohort covers each concrete strategy and annotation variant below. |
| [`Default-Values.md` — Other fields (`field`)](../user/Default-Values.md#other-fields-field) | #318 (closed: make `@Default` a lifecycle method) | `DefaultValuesSpec` covered field defaults but did not provide a readable linked documentary example. | Aligned by this slice with `DefaultValuesDocumentaryTest.defaults a release identifier from its configured name`. |
| [`Default-Values.md` — Delegate fields (`delegate`)](../user/Default-Values.md#delegate-fields-delegate) | #318 (closed: make `@Default` a lifecycle method) | `DefaultValuesSpec` covered delegate defaults without a readable linked documentary example. | Aligned by this slice with `DefaultValuesDocumentaryTest.defaults a component name from its owning container`. |
| [`Default-Values.md` — Arbitrary code (`code`)](../user/Default-Values.md#arbitrary-code-code) | #318 (closed: make `@Default` a lifecycle method) | `DefaultValuesSpec` covered code defaults without a readable linked documentary example. | Aligned by this slice with `DefaultValuesDocumentaryTest.derives a normalized release identifier with default code`. |
| [`Default-Values.md` — Default as lifecycle annotation](../user/Default-Values.md#default-as-lifecycle-annotation) | #318 (closed: make `@Default` a lifecycle method) | `DefaultValuesSpec` covered lifecycle defaults without a readable linked documentary example. | Aligned by this slice with `DefaultValuesDocumentaryTest.runs a default lifecycle method when a value is absent`. |
| [`Default-Values.md` — `@DefaultValues` annotation](../user/Default-Values.md#defaultvalues-annotation) | #361, #370 | `DefaultValuesDocumentaryTest` | Grouping exception: this heading introduces the annotation family; its class, field, closure/coercion, `ignoreUnknownFields`, `valueTarget`, and `@DefaultApply` child headings each have a directly linked documentary feature. |
| [`Default-Values.md` — `@DefaultValues` class annotation](../user/Default-Values.md#class-annotation) | #361 | `DefaultValuesSpec` covered class annotations without a readable linked documentary example. | Aligned by this slice with `DefaultValuesDocumentaryTest.applies a default-values annotation to a configuration class`. |
| [`Default-Values.md` — `@DefaultValues` field annotation](../user/Default-Values.md#field-annotation) | #361 | `DefaultValuesSpec` covered field annotations without a readable linked documentary example. | Aligned by this slice with `DefaultValuesDocumentaryTest.applies a default-values annotation to a child field`. |
| [`Default-Values.md` — Closure and coercion](../user/Default-Values.md#closure-and-coercion) | #361 | `DefaultValuesSpec` covered closure execution and coercion without a readable linked documentary example. | Aligned by this slice with `DefaultValuesDocumentaryTest.evaluates a default-values closure and coerces its result`. This slice replaces the previous unexecutable `HomeDefaults` example, whose undeclared `label` annotation member and `Window.owner` property did not match the declared types, with the tested `CapacityDefaults` coercion path. |
| [`Default-Values.md` — `ignoreUnknownFields`](../user/Default-Values.md#ignoreunknownfields) | #361 | `DefaultValuesSpec` covered strict and lenient annotation members without a readable linked documentary example. | Aligned by this slice with `DefaultValuesDocumentaryTest.ignores unmatched default-values members when configured`. |
| [`Default-Values.md` — `valueTarget`](../user/Default-Values.md#valuetarget) | #370 | `DefaultValuesSpec` covered remapping and its validation boundary without a readable linked documentary example. | Aligned by this slice with `DefaultValuesDocumentaryTest.maps a concise annotation value to a default field`; the invalid annotation shapes remain focused compiler-boundary coverage. |
| [`Default-Values.md` — `@DefaultApply`](../user/Default-Values.md#defaultapply) | #370 | `DefaultValuesSpec` covered application defaults without a readable linked documentary example. | Aligned by this slice with `DefaultValuesDocumentaryTest.applies default configuration to a child object`. |
| [`Templates.md` — Creating templates](../user/Templates.md#creating-templates) | #491 audit selection; #322 for `CreateFrom` | `TemplatesSpec`, `BoundTemplatesSpec` | Aligned with `TemplatesDocumentaryTest.creates an unkeyed reusable template without lifecycle callbacks` and `creates a template from a DelegatingScript file`. |
| [`Templates.md` — `copyFrom()`](../user/Templates.md#copyfrom) | #491 audit selection | `TemplatesSpec`, `BoundTemplatesSpec` | Aligned with `TemplatesDocumentaryTest.copies a template into one completed service configuration`. |
| [`Templates.md` — Template.With() and anonymous templates](../user/Templates.md#templatewith) | #376 (closed: Closure Templates) | `BoundTemplatesSpec` | Aligned with `TemplatesDocumentaryTest.applies one scoped template to multiple service configurations` and `applies named values through an anonymous scoped template`. |
| [`Templates.md` — Collection factories](../user/Templates.md#templates-for-collection-factories) | #82 (closed: collection factory closure contract) | `BoundTemplatesSpec` | Aligned with `TemplatesDocumentaryTest.applies one collection-factory template to every created server`. |
| [`Templates.md` — Template.WithAll()](../user/Templates.md#templatewithall) | #376 (closed: Closure Templates) | `BoundTemplatesSpec` | Aligned with `TemplatesDocumentaryTest.applies templates for multiple configuration types in one scope`. |
| [`Templates.md` — Abstract classes](../user/Templates.md#templates-for-abstract-classes) | #491 audit selection | `TemplatesSpec`, `BoundTemplatesSpec` | Aligned with `TemplatesDocumentaryTest.creates a template implementation for an abstract configuration type`. |
| [`Templates.md` — Order of precedence](../user/Templates.md#order-of-precedence) | #491 audit selection | `TemplatesSpec`, `BoundTemplatesSpec` | Aligned with `TemplatesDocumentaryTest.lets child templates and explicit configuration override parent defaults`. |
| [`Templates.md` — `applyLater`](../user/Templates.md#applylater-and-templates) | #376 (closed: Closure Templates) | `BoundTemplatesSpec`, `ApplyLaterBoundarySpec` | Aligned with `TemplatesDocumentaryTest.replays a template applyLater recipe for each completed configuration`; rejection and capture-boundary cases remain focused regression coverage. |
| [`Convenience-Factories.md` — Script classes](../user/Convenience-Factories.md#script-classes) | #491 audit selection | `ConvenienceFactoriesSpec` | Aligned with `ConvenienceFactoriesDocumentaryTest.loads a completed deployment from a script class`. |
| [`Convenience-Factories.md` — Delegating Scripts](../user/Convenience-Factories.md#delegating-scripts) | #491 audit selection | `ConvenienceFactoriesSpec` | Aligned with `ConvenienceFactoriesDocumentaryTest.uses a DelegatingScript class as keyed configuration content`. |
| [`Convenience-Factories.md` — Script and delegating script for collections and maps](../user/Convenience-Factories.md#script-and-delegating-script-for-collections-and-maps) | #198 | `ConvenienceFactoriesSpec`, `AsBuilderSpec` | Aligned with `ConvenienceFactoriesDocumentaryTest.applies DelegatingScript recipes to list and map relationship factories`; materializing regular Script rejection remains boundary coverage. |
| [`Convenience-Factories.md` — Text](../user/Convenience-Factories.md#text) | #114 | `ConvenienceFactoriesSpec` | Aligned with `ConvenienceFactoriesDocumentaryTest.loads keyed configuration from text`. The optional class-loader overload is an invocation variation, not a separate tutorial path. |
| [`Convenience-Factories.md` — File or URL](../user/Convenience-Factories.md#file-or-url) | #491 audit selection | `ConvenienceFactoriesSpec`, `ScriptTest` | Aligned with `ConvenienceFactoriesDocumentaryTest.derives a keyed configuration name from a file or URL`. The custom key-provider overload remains focused behavior coverage in `ScriptTest`. |
| [`Convenience-Factories.md` — Classpath](../user/Convenience-Factories.md#classpath) | #491 audit selection | `ConvenienceFactoriesSpec` | Aligned with `ConvenienceFactoriesDocumentaryTest.discovers a deployment entry point from its classpath marker`; abstract and DelegatingScript entry-point variants remain focused coverage. |
| [`Convenience-Factories.md` — Map](../user/Convenience-Factories.md#map) | #359 | `FactoryTest`, `AsBuilderSpec` | Aligned with `ConvenienceFactoriesDocumentaryTest.adapts external map keys in a custom factory`. `Create.AsBuilder().FromMap` remains an active-session extension boundary rather than a standalone tutorial. |
| [`Converters.md` — Field-Based Converters](../user/Converters.md#field-based-converters) | #148 | `ConverterSpec` | Aligned with `ConvertersDocumentaryTest.converts timestamp input for a simple field and map entry`. The zero-argument and multi-argument overloads, DSL-object source-visibility rule, and generated-method reflection remain focused behavior or integration boundaries. |
| [`Converters.md` — Factory Method Converters](../user/Converters.md#factory-method-converters) | #148 | `ConverterSpec` | Aligned with `ConvertersDocumentaryTest.uses a named factory method as an owned DSL relationship creator`, which demonstrates both root and owning-Model invocation. Prefix variations, ambiguous signatures, keyed/list/map variants, generic-placeholder behavior (#243), and DelegatingScript collection input (#198) remain focused coverage. |
| [`Converters.md` — Factory Classes](../user/Converters.md#factory-classes) | #148 | `ConverterSpec` | Aligned with `ConvertersDocumentaryTest.uses a converter factory class for convention and annotation-based inputs`. Field-local registration, subtype discovery, and collection-factory propagation (#300, #319) remain focused extension coverage. |
| [`Converters.md` — Customization](../user/Converters.md#customization) | #148 | `ConverterSpec` | Aligned with `ConvertersDocumentaryTest.uses an opt-in URI constructor as a converter`. Method include/exclude filters and prefix changes remain API-reference and focused discovery coverage. |
| [`Validation.md` — On Classes](../user/Validation.md#on-classes) | #276 | `ValidationSpec`, `ValidationDocumentaryTest` | Aligned with `ValidationDocumentaryTest.validates unannotated fields when a class is marked for validation`. The class-annotation conversion remains focused transformation coverage in `ValidationSpec`. |
| [`Validation.md` — On Fields](../user/Validation.md#on-fields) | #491 audit selection | `ValidationSpec`, `ValidationDocumentaryTest` | Aligned with `ValidationDocumentaryTest.validates a numeric field with a closure` and `reports an explicit validation message for a field`. Groovy-truth spelling, `Ignore`, named/assertion closures, inherited annotations, Templates, and compiler-shape failures remain focused behavior or boundary coverage. |
| [`Validation.md` — `@Required` and `@Optional`](../user/Validation.md#required-and-optional) | #221, #409 | `ValidationSpec`, `ValidationDocumentaryTest` | Aligned with `ValidationDocumentaryTest.uses Required as the concise Groovy-truth field rule` and `excludes an optional field from class-wide validation`. Message/level permutations remain focused coverage. |
| [`Validation.md` — Suppress Further Issues](../user/Validation.md#suppress-further-issues) | #407 | `ValidationSpec.suppresses a later non-error issue for one member` | Already aligned: the feature carries `@Issue("407")`, `@Tag("documentary")`, and an `@See` to this heading. |
| [`Inheritance.md` — Choosing a derived implementation](../user/Inheritance.md#choosing-a-derived-implementation) | #491 audit selection | `InheritanceSpec` | Aligned with `InheritanceDocumentaryTest.configures a derived project through an unkeyed field`. |
| [`Inheritance.md` — Keyed inheritance](../user/Inheritance.md#keyed-inheritance) | #130 | `InheritanceSpec` | Aligned with `InheritanceDocumentaryTest.configures a keyed derived project through an inherited key`. |
| [`Inheritance.md` — Key hierarchy constraints](../user/Inheritance.md#key-hierarchy-constraints) | #130 | `InheritanceSpec` | Intentional exception: this rejection boundary is not a tutorial path. `InheritanceSpec` retains the valid abstract-parent case and invalid keyed-child declarations as focused coverage. Final/abstract typed-factory availability is likewise a focused `InheritanceSpec` API boundary; #138's two-children-before-parent compilation regression remains there rather than becoming a user-facing example. |

## Campaign reconciliation

The #491 campaign ledger was reconciled against `master` at `c9430e49` (2026-07-29). Every mapping below names the
current page heading, documentary class and method, and governing issue evidence. “Merged” means the documented
evidence is present on `master`; “already aligned” means a prior merged change already supplied that evidence. The
exceptions are deliberate boundaries, not missing-test claims.

| Page / heading(s) | Documentary evidence and governing issue | Reconciled result and boundary |
| --- | --- | --- |
| `Basics.md` — `@Owner`, Owner methods, Transitive owners, Root owners, and Owner converters | `OwnerRelationshipDocumentaryTest`: `assigns each matching owner field for an owned service` (#171), `derives service metadata in an owner method` (#176), `finds the first matching transitive owner in a deployment path` (#49), `makes the root deployment available without a direct owner path` (#491 selection), and `converts an owner into readable service metadata` (#189) | Merged in PR #594. Assignment timing, signature validation, inheritance, wrong-root rejection, role fields, and closure-phase cases remain focused coverage. |
| `Validation.md` — JSR380 Validation; Validation Levels and JSR380; Using the Gradle Plugin | `BeanValidationDocumentaryTest`: `accepts a release with a satisfied Jakarta validation constraint` and `records a Jakarta constraint violation at its payload-selected level`; `BeanValidationGradleDocumentaryTest.schema plugin aligns an optional Bean Validation module to its BOM` (all #491 selection) | Merged in PR #595. Provider/integration mechanics, unknown payload-to-`NONE` mapping, and unsupported standalone/Grab paths remain optional-module boundaries. |
| `Basics.md` — Virtual Fields | `VirtualFieldsDocumentaryTest.configures a virtual field with a concrete article type` (#250) | Merged in PR #596. Setter-only legality and wider polymorphic, generated-API, and lifecycle cases remain focused coverage. |
| `Validation.md` — Validation Levels, Deprecations, and `@Notify` | `ValidationPolicyDocumentaryTest`: `records a warning-level validation result without failing construction` and `reports a documented deprecation only for a manually configured legacy field` (#145); `reports a missing manually configured field` and `uses Notify to replace the default deprecated-field policy` (#407) | Merged in PR #597. `ifUnset`/`ifSet` wording is aligned; verification and JSR380 remain separate selections. |
| `Builder-First-Migration.md` — Compile and Run a Representative Model | `BuilderFirstMigrationDocumentaryTest.builds a representative deployment through one Builder lifecycle` (documentary selection #491; #416 governs the migration behavior) | Merged in PR #599. JSON/#464, Layer 3/#474, Basics, and Validation are outside this migration mapping. |
| `Completed-Object-Support.md` — Stored validation | `CompletedObjectSupportDocumentaryTest.reads stored validation results for a completed deployment` (#491 selection) | Merged in PR #600. It uses the current `getResult()` / `getSubtreeResults()` contract; the `getResults()` bridge remains an API/migration boundary under #550. |
| `FAQ.md` — Why an AST and not just use a ModelBuilder? | `FAQDocumentaryTest.configures a generated Builder from a type-checked model script` (#491 selection) | Merged in PR #601. IDE-completion and library-difference answers remain reference/comparative material. The name/acronym answer is likewise reference-only, without a behavior-bearing owner. |
| `Jackson-Integration.md` — Managed Import | `JacksonImporterDocumentaryTest.imports a completed release through the managed importer` (#491 selection) | Merged in PR #602. The selected Builder lifecycle does not decide #463's importer contract or #464's one-input/enrich/export YAML asymmetry. |
| `Exception-Handling.md` — KlumModelException | `ExceptionHandlingDocumentaryTest.reports the DSL location for a model-creation error` (#491 selection) | Merged in PR #603. `KlumException` remains base-type reference coverage; `KlumSchemaException` is schema-bound focused coverage; `KlumVisitorException` is phase/traversal SPI coverage; `KlumValidationException` belongs to Validation. |
| `Completed-Object-Support.md` — Ownership, paths, and traversal | `CompletedObjectSupportDocumentaryTest.traverses a deployment composition without following linked services` (#491 selection) | Merged in PR #604. Direct owner, relative path, typed `findAll`/`visit`, and `LINK` exclusion are selected; subtree-start and compatibility APIs remain unclaimed, with #550 retaining the bridge boundary. |
| `Advanced-Techniques.md` — Delegation Hints for Builder Closures; Behavior Models and Parameter Hints; Choosing a SAM Interface or Closure | No settled behavior owner | Deferred to #544's pre-RC concept-page readability, style, and usefulness review. This is not a request to manufacture a #491 tutorial; Layer 3/#454 and design, migration, public-API, and policy work remain excluded. |
| `Usage.md` — Schema - Model - Consumer | `UsageDocumentaryTest.loads a configured model through its classpath entry point` (#110; #491 documentary relation) | Merged in PR #605. Gradle setup, manual dependencies, and All-in-One mirrors stay with #456/#469/plugin-policy routing, onboarding, versioning, and hosting. |
| `Basics.md` — `@DSL` and Factory construction | `FactoryConstructionTest.builds a completed deployment configuration with Create.With` (#76) | Already aligned in the earlier merged factory-construction change. `@DSL` is orientation only; no duplicate documentary test is justified. |
| `Basics.md` — Default Implementation | `BasicsDefaultImplementationDocumentaryTest.configures a default implementation through an interface field` (#491 selection; #122 behavior context) | Merged in PR #606. Interface-level defaults, collections/maps, and virtual fields remain focused boundaries. |
| `Basics.md` — DSL Interfaces | `BasicsDslInterfacesDocumentaryTest.configures a DSL interface through a concrete implementation` (#611 behavior; #491 documentary relation) | Aligned by this slice with the concrete `foo(FooImpl) { ... }` authoring path. The #611 fix collects, materializes, and assigns the configured child to the completed parent; default implementation coverage remains a separate #491 mapping. |
| `Basics.md` — Fixed keys | `FixedKeysDocumentaryTest.derives a keyed database name from its server` (#186) | Already aligned in merged PR #607. `FixedKeySpec` retains static/`FieldName`, generated-overload, and invalid-placement boundaries. |
| `Completed-Object-Support.md` — Construction and structural model paths | `CompletedObjectSupportDocumentaryTest.reports distinct construction and structural paths for a completed deployment` (#390; ADR 0006 OS-1/OS-3) | Merged in PR #609. Keep `getResult()` / `getSubtreeResults()`; do not introduce `getResults()`. Opening facade, subtree-start, and compatibility APIs remain unclaimed under the #550 boundary. |
| `Static-Models.md` | No settled behavior owner | Deferred to #544's pre-RC concept-page readability, style, and usefulness review. Builder-first lifecycle, mutator relocation, structural immutability/materialization, collection snapshots, and transient fields are not one documentary contract; closed #56 and ADR 0003 do not establish one. |

## In-scope user-visible DSL inventory

The following maps the current feature-oriented user-documentation corpus to the best local historical issue evidence. `Not yet
aligned` means the documentation and executable behavior exist, but this audit has not yet established a readable,
annotated documentary path. It is a queue for a later #491 slice, not a new behavioral contract.

| Documentation element | Responsible issue evidence | Executable coverage evidence | Status / next audit action |
| --- | --- | --- | --- |
| `Basics.md` — `keyMapping` for Simple Maps | #128 | `TransformSpec`, `BasicsKeyMappingDocumentaryTest` | Aligned with `BasicsKeyMappingDocumentaryTest.derives simple map keys from configured values`. This direct value-to-key mapping is a readable, settled authoring path; the generated-overload shape remains focused coverage in `TransformSpec`. |
| `Basics.md` — Automatic Key determination for DSL-Map entries | #127 | `TransformSpec`, `BasicsKeyMappingDocumentaryTest` | Aligned with `BasicsKeyMappingDocumentaryTest.uses default and configured keys for DSL map entries`. The example proves both the heading's default keyed-child map behavior and its `keyMapping` override; invalid mapping and `LINK` combination coverage remain focused in `TransformSpec`. |
| `Basics.md` — `@Owner`, Owner methods, Transitive owners, Root owners, and Owner converters | #171, #176, #49, #189; #491 audit selection for the root-owner heading | `OwnerReferencesSpec`, `RWClassSpec`, `OwnerRelationshipDocumentaryTest` | Aligned as one owner-relationship cohort. `OwnerRelationshipDocumentaryTest` demonstrates matching direct owner fields, owner methods, the first matching transitive owner, root-owner access without a direct owner path, and owner conversion. Assignment timing, method-signature validation, inheritance, wrong-root-type rejection, role fields, and closure-phase behavior remain focused `OwnerReferencesSpec`/`RWClassSpec` coverage. |
| `Alternatives-Syntax.md` — Alternatives Syntax, Naming Strategies, Defining Explicit Names for a Field, Explicit Short Names for Subclasses, Strip Common Suffixes, and Default: Derived from the Class Name | #491 audit selection; #77 and #270 for focused historical collection-template/converter evidence; #544 for the existing `stripSuffix` example | `AlternativesSpec`, `AlternativesDocumentaryTest` | Aligned as one dedicated documentary cohort. `AlternativesDocumentaryTest` demonstrates default class-derived names, field-local literal mappings, and deliberate subtype `shortName`s; `AlternativesSpec.uses stripped suffixes for alternative method names` remains the existing `stripSuffix` evidence. The page and Naming Strategies headings are grouping/orientation headings, not independent API paths. The required same-compilation-run and unique-simple-name rules, literal-map rejection cases, and source-visible converter projection remain focused compiler or Builder-boundary coverage; #391 continues to own package/API boundary work. |
| `Convenience-Factories.md` — script classes, DelegatingScript recipes, collection/map factories, text, file/URL, classpath, and maps | #114, #198, #359; #491 audit selection for historical seams without one governing issue | `ConvenienceFactoriesSpec`, `ScriptTest`, `FactoryTest`, `AsBuilderSpec`, `ConvenienceFactoriesDocumentaryTest` | Aligned as one dedicated documentary cohort. Class-loader and custom key-provider overloads, abstract entry points, active-session `Create.AsBuilder().FromMap`, and materializing Script rejection remain focused behavior or boundary coverage rather than tutorial paths. |
| `Converters.md` — Field-Based Converters, Factory Method Converters, Factory Classes, and Customization | #148; #198, #243, #300, #319 for focused boundary coverage | `ConverterSpec`, `ConvertersDocumentaryTest` | Aligned as one dedicated documentary cohort. `includeConstructors` has a readable opt-in tutorial; method filters, overload, visibility, generic, ambiguity, collection-factory, and script boundaries remain focused coverage rather than separate tutorials. |
| `Copy-Strategies.md` — Single Object, Collections, Maps, and Nested Annotations | #309; #581 for packaged overwrite-strategy validation | `CopyHandlerTest`, `OverwriteStrategyTest`, `CopyStrategiesDocumentaryTest` | Aligned as one dedicated documentary cohort with `merges a nested service configuration from a template`, `adds template roles to a service configuration`, `merges environment map values from a template`, and `applies the packaged Helm copy policy to a deployment`. |
| `Copy-Strategies.md` — Copy source protocol, strategy lookup, Single Object `INHERIT`/`REPLACE`/`ALWAYS_REPLACE`/`SET_IF_NULL`/`MERGE`, Collections `INHERIT`/`ADD`/`REPLACE`/`SET_IF_EMPTY`/`ALWAYS_REPLACE` and its `null` setter example, Maps `INHERIT`/`FULL_REPLACE`/`ALWAYS_REPLACE`/`SET_IF_EMPTY`/`MERGE_KEYS`/`MERGE_VALUES`/`ADD_MISSING`, and Missing field handling | #36, #309, #348, #374, #400 | `CopyHandlerTest`, `CopyHandlerRuntimeTest`, `OverwriteStrategyTest` | Deliberate exception: these source-session, precedence, compatibility, and exhaustive API-reference rules retain focused coverage rather than individual tutorial methods. |
| `Default-Values.md` — Other fields (`field`) | #318 | `DefaultValuesSpec`, `DefaultValuesDocumentaryTest` | Aligned with `DefaultValuesDocumentaryTest.defaults a release identifier from its configured name`. The Layer 3 annotation variants remain separate follow-ups under #361 and #370. |
| `Inheritance.md` — Choosing a derived implementation and Keyed inheritance | #491 audit selection; #130 for keyed inheritance | `InheritanceSpec`, `InheritanceDocumentaryTest` | Aligned as one dedicated documentary cohort. Final/abstract typed-factory availability, illegal key-hierarchy declarations, and #138's two-children-before-parent compilation regression remain focused boundary coverage rather than tutorial paths. |
| `Templates.md` — Templates cohort: creation, copying, scoped/anonymous/collection templates, WithAll, abstract classes, precedence, and `applyLater` | #491 audit selection; #82, #322, #376 | `TemplatesSpec`, `BoundTemplatesSpec`, `ApplyLaterBoundarySpec`, `TemplatesDocumentaryTest` | Aligned as one dedicated documentary cohort. Internal identity, relationship rejection, serialization, and capture-rejection boundaries remain focused regression coverage rather than tutorial paths. |
| `Validation.md` — On Classes, On Fields, and `@Required`/`@Optional` | #276; #491 audit selection; #221, #409 | `ValidationSpec`, `ValidationDocumentaryTest` | Aligned as the core declarative-validation cohort. The class-wide, field-closure/message, required, and optional examples now carry readable documentary evidence. |
| `Validation.md` — On Methods and Custom Issues | #626, implemented by merged PR #627; #491 documentary selection; #406 remains the pending lifecycle-only compiler boundary | `KlumValidationReporterTest`, `ValidationDocumentaryTest`, `ValidationSpec` | Aligned with `KlumValidationReporterTest.reports a current validation-method issue through the static Groovy property` and `ValidationDocumentaryTest.reports failed validation-method assertions at their configured levels`. The public reporter and assertion path preserve lifecycle semantics; member targeting, phase stacking, and #406 remain focused boundary coverage. |
| `Validation.md` — On Inner Classes and Custom Issues on Inner Classes | #626, implemented by merged PR #627; historical #415 | `KlumValidationReporterTest`, `ValidationSpec` | Aligned by the same reporter contract: validation classes use `klumValidation` directly and no longer inherit `ValidatorBase`. Discovery/visibility, constructors, inheritance/overrides, and levels remain focused coverage. |
| `Validation.md` — Validation of Nested Objects | #624; historical #25, #125 | `ValidationSpec`, `ValidationDocumentaryTest` | Aligned with `ValidationDocumentaryTest.validates a child against parent state configured after the child`. The readable `@Owner` path proves deferred complete-tree validation when the parent value is configured later in the same root DSL block. Nested list/map traversal and broader phase-order behavior remain focused `ValidationSpec` coverage. |
| `Validation.md` — Validation Levels, Deprecations, and `@Notify` | #145, #407 | `ValidationSpec`, `ValidationPolicyDocumentaryTest` | Aligned by the merged policy cohort. `ValidationPolicyDocumentaryTest` maps warning-level stored results, manual-only legacy deprecation, an unset notification, and `@Notify` precedence; verification remains a separate operational boundary. |
| `Validation.md` — Suppress Further Issues | #407 | `ValidationSpec.suppresses a later non-error issue for one member` | Already aligned documentary feature; deliberately excluded from this cohort. |
| `Validation.md` — Validation and Verify, and Skipping Verification | #624; historical #381 | `ValidationSpec`, `ValidationDocumentaryTest` | Aligned with `ValidationDocumentaryTest.verifies stored results without rerunning validators`. It sets `klum.validation.skipVerify`, reads the stored result through `KlumObjectSupport`, and verifies that `verify()` applies the stored issues without another validation run. `failOnLevel`, deprecated `Validator.verifyStructure`, and broader system-property boundaries remain focused coverage. |
| `Validation.md` — JSR380 Validation, Validation Levels and JSR380, and Using the Gradle Plugin | #491 audit selection; Bean-validation module contract | `BeanValidationDocumentaryTest`, `BeanValidationGradleDocumentaryTest`, `JSR380ValidatorTest`, `TransformedBeanValidationTest` | Aligned by the merged optional-module cohort. Provider/integration mechanics, unknown payload-to-`NONE` mapping, and unsupported standalone/Grab paths remain focused integration boundaries. |
| `Model-Phases.md` — Lifecycle annotations, Creation, AutoCreate/Owner/AutoLink/Default/PostTree, and Validation | #491 audit selection for the settled Builder-first lifecycle cohort | `LifecycleSpec`, `ModelPhasesDocumentaryTest` | Aligned with `ModelPhasesDocumentaryTest.runs a deployment lifecycle on Builders before completing its model`. This page explicitly makes the class its exception to the normal per-heading abbreviated-example convention: one linked end-to-end example keeps the lifecycle readable while the test retains exact per-heading links. It asserts the documented Builder-to-completed-model transition without changing phase order. #64's child-`@PostCreate` regression remains focused `LifecycleSpec` coverage. |
| `Model-Phases.md` — Instantiate | #491 audit selection; ADR 0003 | `BuilderFirstSpec`, `ModelPhasesDocumentaryTest` | Aligned with `ModelPhasesDocumentaryTest.materializes a release plan into an independent completed snapshot`. The broader cyclic-relationship, collection-kind, constructor, and source-initializer cases remain focused Builder-first coverage. |
| `Model-Phases.md` — ApplyLater and `applyLater` Methods | #376 | `LifecycleSpec`, `ApplyLaterBoundarySpec`, `ModelPhasesDocumentaryTest` | Aligned with `ModelPhasesDocumentaryTest.applies a deferred deployment setting before automatic lifecycle work`. Template replay, captured-value rules, past/current phase behavior, and post-materialization rejection remain focused boundary coverage. |
| `Model-Phases.md` — PhaseActions, Early Validation, Verify, Completion, Error handling, and Methods that modify datastructures | ADR 0003/0008; #138 and #376 focused regression evidence | `PhaseActionTypingTest`, `BuilderFirstSpec`, `LifecycleSpec`, `ApplyLaterBoundarySpec` | Deliberate focused/boundary exception: these headings describe an extension SPI, provisional diagnostics, verification/cleanup mechanics, failures, or a concurrent-modification workaround. They have no independent stable authoring happy path; keep their executable evidence focused rather than inventing tutorials. #138's reverse-compilation lifecycle regression remains `LifecycleSpec` coverage. |
| `Layer3.md` | #454 owns terminology/documentation placement; #474 owns the existing optional-link example | `OptionalLinkRelationshipTest` | One narrow section aligned; defer broader example changes until #454's required terminology grilling. |
| `Completed-Object-Support.md` — Construction and structural model paths; Ownership, paths, and traversal; Stored validation | #390 / ADR 0006 OS-1/OS-3 for construction; #491 selections; #550 compatibility boundary | `CompletedObjectSupportDocumentaryTest` | Aligned with the three precisely linked methods recorded in the campaign reconciliation. Opening facade, subtree-start, and Compatibility APIs remain unclaimed; retain `getResult()` / `getSubtreeResults()` rather than reintroducing `getResults()`. |
| `Builder-First-Migration.md` — Compile and Run a Representative Model | #416 migration behavior; #491 documentary selection | `BuilderFirstMigrationDocumentaryTest`, `BuilderFirstSpec` | Aligned with the linked single-Builder-lifecycle path. JSON/#464, Layer 3/#474, Basics, and Validation remain outside this mapping. |
| `Factory-Classes.md` — Custom Creator Classes | #76 (closed: move creator methods into a creator class) | `FactoryTest.allow overriding of factory base class with implicit factory` | Aligned by #544 with the documented nested `Factory` path. The feature carries the class-level `@Issue("76")`, plus `@Tag("documentary")` and `@See`; generic and abstract factory variants remain focused coverage. |
| `Behind-the-Curtain.md` | #431 / ADR 0004 for Builder projection; #463/#464 for managed Jackson integration | `BuilderProjectionSpec`, `BuilderProjectionWithFilesSpec`, and Jackson importer tests | Deliberate reference exception: this page explains implementation boundaries and migration diagnostics, not a stable authoring entry point. It links readers to the supported Converters, Factory Classes, Alternatives Syntax, Jackson Integration, and Builder-first migration paths; its focused tests must not be repurposed as a tutorial merely to create a documentary mapping. |
| `Javadoc.md` — Javadoc for models | #197 (closed: add AnnoDocimal Javadocs) | `AnnoDocTest.generated getters document model and Builder values` | Aligned by #544 with the stable Schema-authoring path: property comments are projected to generated Model and Builder accessors. The feature carries the class-level `@Issue("197")`, plus `@Tag("documentary")` and `@See`; templates and overload rewriting remain focused compiler evidence. |
| `Terms.md`, `Why-aC-is-not-enough.md` | Reference material, not one discrete feature contract | Focused behavior tests exist where applicable | Reference-material classification: no feature documentary test is required merely because the page is validated or contains examples. `Exception-Handling.md` and the behavior-bearing FAQ answer have the selected mappings recorded above; FAQ naming remains reference-only. |
| `Domain-First-Modeling.md` — Smart-home journey | #469 onboarding; its fixture feature is driven by #471 | `SmartHomeJourneyDocumentaryTest.loads a floorplan-specific Model and lets a generic client inspect every window` | Explicit #469 mapping/exemption: the page already names the readable journey test, which carries `@Tag("documentary")` and an `@See` to its heading. Do not add a separate #491 feature-documentary test. |
| `Target-Contract-Modeling.md` — Executable Helm journey | #469 onboarding; its fixture feature is driven by #472 | `HelmTargetContractDocumentaryTest.generates human-readable #release.name Helm values that conform to the golden contract` | Explicit #469 mapping/exemption: the page already names the readable journey test, which carries `@Tag("documentary")` and an `@See` to its heading. Do not add a separate #491 feature-documentary test. |

### `Basics.md` heading queue and boundaries

This is the stable user-facing split of the broad Basics page. The two selected `keyMapping` headings above are complete;
factory construction remains covered by `FactoryConstructionTest`. Each remaining entry is durable #491 queue/boundary
evidence, not an instruction to add a documentary test without a separate coherent selection.

| Stable heading(s) | Current evidence and boundary | Next action |
| --- | --- | --- |
| `@DSL` | Orientation heading; its stable introductory authoring path is already aligned through Factory construction and `FactoryConstructionTest`. #35 serialization and #21/#22 generated-helper rules remain implementation shape. | Do not duplicate factory evidence. |
| Lifecycle Methods; `copyFrom()` Method; `equals()` Method; `hashCode()` | Lifecycle, copying, and model-shape contracts overlap Model Phases, Copy Strategies, Templates, and Builder-first migration cohorts. | Do not duplicate those cohorts. |
| Field setter; Field setter for simple fields; Setter for simple collections | General generated-Builder accessors have broad focused coverage but no one settled historical issue for a new tutorial path. | Select only with an unambiguous contract. |
| Setters and closures for DSL-Object Fields; Polymorphic DSL members; Typed factory methods; Existing completed values | Builder lifecycle, polymorphic composition, and completed-object rejection are cross-cutting Builder-first and migration boundaries. | Defer; do not change #454/#484 migration terminology. |
| Virtual Fields | #250 establishes the selected concrete-article path through `VirtualFieldsDocumentaryTest`; setter-only legality remains focused compiler coverage. | Aligned; retain broader polymorphic/generated-API/lifecycle cases as boundaries. |
| Default Implementation | `BasicsDefaultImplementationDocumentaryTest` selects the `@DSL(defaultImpl=...)` interface-field path. Interface-level default, collections/maps, and virtual fields remain focused boundaries. | Aligned; do not widen the cohort. |
| Collections of DSL Objects; Polymorphic collection members | #58 covers collection creation; polymorphism and ownership are lifecycle boundaries. The nested Automatic Key determination heading is selected above. | Keep remaining collection/polymorphism coverage focused. |
| On collections | #56 snapshot/read-only behavior and #249 `EnumSet` are correctness and collection-kind boundaries. | Retain focused coverage. |
| The `@Key` Annotation | Keyed construction is already part of factory construction; inheritance and fixed-key variants have separate contracts. | Do not duplicate factory evidence. |
| Field Types; PROTECTED; BUILDER; TRANSIENT; IGNORED; LINK; OPTIONAL_LINK | #80, #126, and #172 cover a mix of generated access, construction-only state, and aggregation boundaries; OPTIONAL_LINK overlaps Layer 3 terminology. | Defer; preserve #454 boundary. |
| DSL Interfaces | #611 establishes the concrete implementation path through `BasicsDslInterfacesDocumentaryTest`; #491 records its documentary relation. Default implementations remain covered separately. | Aligned; retain historical interface/default-implementation context as focused boundaries. |
| Fixed keys | #186's settled keyed-member convenience path is mapped by `FixedKeysDocumentaryTest`; static/`FieldName`, generated-overload, and invalid-placement cases remain focused coverage. | Aligned; do not duplicate key-mapping evidence. |

## Explicit exclusions and coordination boundaries

- `Jackson-Integration.md` — Managed Import has the selected #491 mapping above. #463 retains the importer-contract
  boundary, and #464 retains YAML read/enrich/export asymmetry; this audit does not alter those contracts, examples, or
  tests.
- `Usage.md` — Schema - Model - Consumer has the selected #110 mapping above. Its Gradle/manual/All-in-One setup,
  together with `Home.md`, `Gradle-Plugins.md`, release/migration navigation, and adopter journeys, remain #456/#469
  versioning, hosting, plugin-policy, or onboarding coordination surfaces.
- The #469 domain-first and target-contract journeys are explicit exceptions to that general coordination rule: their
  named documentary tests are the readable executable evidence for those journey pages, without turning every journey
  step or code fence into a separate #491 feature test.
- `Why-aC-is-not-enough.md` is reference material about the testing boundary. Its classification does not assert that
  the page lacks validation; it only says that the page itself is not a discrete feature contract requiring a
  feature-documentary test.
- `Converters.md` — **Customization** has one stable `includeConstructors` tutorial. `includeMethods`,
  `excludeMethods`, and `excludeDefaultPrefixes` remain API-reference selection controls with focused
  discovery/selection coverage in `ConverterSpec`, not separate documentary examples.
- `Roadmap.md`, `Migration.md`, `_Sidebar.md`, and `_Footer.md` are release, migration, or navigation material rather
  than user-visible DSL feature elements. They do not require a feature documentary test of their own.
- #454's Layer 3 terminology rewrite remains a product/documentation-placement dependency. The existing #474 example is
  retained, but this audit does not broaden it.
- `Advanced-Techniques.md` and `Static-Models.md` are explicit #544 pre-RC concept-page review deferrals, not unresolved
  generic ownership entries. Their readability, style, and usefulness must be reviewed without creating a #491 test.

## Follow-up queue for human action

No GitHub issues were created by this local-only slice. Before a subsequent #491 implementation slice, a maintainer
should choose the next stable documentation heading and either confirm its governing historical issue or create a
focused follow-up for an unclear contract. The Templates and Convenience Factories cohorts are complete; the Layer 3
annotation variants in `Default-Values.md` remain bounded by #361/#370. Completed Object Support now has only its
explicitly unclaimed facade, subtree-start, and compatibility/API boundaries; Advanced Techniques and Static Models
belong to #544's concept-page review, while the remaining legacy-technique pages still need ownership confirmation.
