name: Probe reusable Pages writer secret access

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  call-probe:
    name: Call reusable protected-environment probe
    uses: ./.github/workflows/probe-pages-writer-secrets-reusable.yml
